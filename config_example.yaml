database:
  url: "mysql+mysqlconnector://root:PASSWORD@localhost/network_ai_v2"

model:
  path: "anomaly_model_v2.pkl"
  threshold_load: 20
  threshold_reliability: 200
  threshold_errors: 10

collector:
  interval: 10

discord:
  token: "YOUR_BOT_TOKEN_HERE"
  channel_id: YOUR_CHANNEL_ID_HERE